node_modules/
dist/
*.tsbuildinfo
.DS_Store
.vscode/
.idea/
coverage/
*.log
# Never commit an auth token
.npmrc

# Playwright test output (from dogfood runs that leak into the repo root)
/test-results/
/playwright-report/
/playwright/.cache/

# 0.19.0-α.13 — docs auto-copied into packages/cli/ at prepublishOnly
# time so `slowcook docs <topic>` finds them after npm install. The
# canonical copies are at the repo root; the cli-side copies are build
# artifacts and shouldn't be committed.
/packages/cli/REPORTING.md
/packages/cli/AGENTS.md

# local OAuth-app credentials — never commit (client secret lives here; device flow doesn't even use it)
github-slowcook-app-secret.txt
*-app-secret.txt
